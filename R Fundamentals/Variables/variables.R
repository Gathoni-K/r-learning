# Variables are containers storing data values

# Declaring variables

a <- 10
# assigning a value of 10 to a
a
# outputs the value of the variable a which is 10.

# Rules for assigning variables: start with a letter, case-sensitive, no spaces, underscores or periods used to separate words, not start with a number

# Multiple variables

# assign the same value to multiple variables in one line
var1 <- var2 <- var3 <- "Orange"

# Print variable values
var1
var2
var3